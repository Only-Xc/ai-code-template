# Agent 指令

在本仓库开始任何任务前，必须完整阅读 `docs/coding-rules.md`，并在所有代码、测试和文档变更中遵守其中的架构规则。

## 修改范围约束

执行任何修改时，必须将变更范围限制在用户明确指定的文件，以及为完成该任务必需的强关联文件。

强关联文件包括：

- 被修改代码直接依赖、调用或导入的文件。
- 与修改内容对应的测试文件。
- 与修改内容对应的配置、迁移或文档文件。
- 因重命名、移动或删除文件而必须同步更新的引用方。

禁止主动修改与当前任务无直接关系的文件，即使发现这些文件存在格式问题、旧代码、重构机会或其他可改进点。

如果预计需要修改的文件数量较多，或需要扩大到用户未指定且关联性不明显的文件，必须先停止并向用户说明原因、影响范围和拟修改文件清单，等待确认后再继续。
